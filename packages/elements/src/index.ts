import React from 'react';
import ReactDOM from 'react-dom';

import { Editor } from '@maily-to/core';

window.MT = {};
window.MT.React = React;
window.MT.ReactDOM = ReactDOM;
window.MT.Editor = Editor;
